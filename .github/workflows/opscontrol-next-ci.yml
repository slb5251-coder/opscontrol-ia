name: OPSControl IA Next CI

on:
  pull_request:
    paths:
      - 'opscontrol-next/**'
      - '.github/workflows/opscontrol-next-ci.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: opscontrol-next
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: opscontrol-next/package-lock.json
      - run: npm install
      - run: npm run build
